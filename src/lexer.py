#!/usr/bin/env python3
"""
ЛЕКСИЧЕСКИЙ АНАЛИЗАТОР ПСЕВДОКОДА
Лабораторная работа №1

Основной модуль лексического анализатора, реализующий разбор исходного кода
на псевдокоде на токены с определением их типа и позиции.
"""

import re
import os
import sys
from typing import List, Dict, Any

class PseudocodeLexer:
    """
    Лексический анализатор для учебного языка псевдокода.
    
    Осуществляет разбиение исходного кода на токены с определением:
    - типа токена (ключевое слово, оператор, идентификатор и т.д.)
    - значения токена
    - позиции в исходном коде (строка, столбец)
    """
    
    # Определение токенов в порядке приоритета (от более специфичных к общим)
    TOKEN_SPECIFICATION = [
        # Многосимвольные операторы (должны быть первыми!)
        ('LEQ',       r'<='),      # Меньше или равно
        ('GEQ',       r'>='),      # Больше или равно  
        ('EQ',        r'=='),      # Равно
        ('NEQ',       r'!='),      # Не равно
        ('AND',       r'&&'),      # Логическое И
        ('OR',        r'\|\|'),    # Логическое ИЛИ
        
        # Односимвольные операторы и разделители
        ('LT',        r'<'),       # Меньше
        ('GT',        r'>'),       # Больше
        ('ASSIGN',    r'='),       # Присваивание
        ('NOT',       r'!'),       # Логическое НЕ
        ('PLUS',      r'\+'),      # Сложение
        ('MINUS',     r'-'),       # Вычитание
        ('MUL',       r'\*'),      # Умножение
        ('DIV',       r'/'),       # Деление
        ('MOD',       r'%'),       # Остаток от деления
        ('COMMA',     r','),       # Разделитель аргументов
        ('SEMI',      r';'),       # Конец инструкции
        ('LBRACE',    r'\{'),      # Начало блока
        ('RBRACE',    r'\}'),      # Конец блока
        ('LPAREN',    r'\('),      # Открывающая скобка
        ('RPAREN',    r'\)'),      # Закрывающая скобка
        
        # Ключевые слова
        ('IF',        r'\bif\b'),      # Условный оператор
        ('ELSE',      r'\belse\b'),    # Альтернативная ветка
        ('WHILE',     r'\bwhile\b'),   # Цикл while
        ('FOR',       r'\bfor\b'),     # Цикл for
        ('IN',        r'\bin\b'),      # Оператор вхождения
        ('RANGE',     r'\brange\b'),   # Функция диапазона
        ('PRINT',     r'\bprint\b'),   # Вывод на экран
        
        # Литералы и идентификаторы
        ('STRING',    r'"[^"]*"'),     # Строковые литералы
        ('NUMBER',    r'\d+'),         # Целочисленные литералы
        ('ID',        r'[a-zA-Z_][a-zA-Z_0-9]*'),  # Идентификаторы
        
        # Служебные токены
        ('COMMENT',   r'#.*'),         # Комментарии
        ('NEWLINE',   r'\n'),          # Перевод строки
        ('SKIP',      r'[ \t]+'),      # Пробелы и табуляции
        ('MISMATCH',  r'.'),           # Любой другой символ (ошибка)
    ]
    
    def __init__(self):
        """Инициализация лексического анализатора."""
        # Компилируем регулярное выражение для всех токенов
        token_regex = '|'.join(f'(?P<{name}>{pattern})' for name, pattern in self.TOKEN_SPECIFICATION)
        self.pattern = re.compile(token_regex)
    
    def tokenize(self, code: str) -> List[Dict[str, Any]]:
        """
        Разбивает исходный код на токены.
        
        Args:
            code (str): Исходный код на псевдокоде
            
        Returns:
            List[Dict[str, Any]]: Список токенов, каждый из которых содержит:
                - type: тип токена
                - value: значение токена
                - line: номер строки
                - column: позиция в строке
                
        Raises:
            RuntimeError: При обнаружении неожиданного символа
        """
        tokens = []
        line_num = 1
        line_start = 0
        
        for match in self.pattern.finditer(code):
            kind = match.lastgroup
            value = match.group()
            column = match.start() - line_start
            
            # Обработка специальных случаев
            if kind == 'SKIP':
                continue  # Пропускаем пробелы
            elif kind == 'NEWLINE':
                line_num += 1
                line_start = match.end()
                continue
            elif kind == 'COMMENT':
                continue  # Пропускаем комментарии
            elif kind == 'NUMBER':
                value = int(value)  # Преобразуем числа в int
            elif kind == 'STRING':
                value = value[1:-1]  # Убираем кавычки у строк
            elif kind == 'MISMATCH':
                raise RuntimeError(f'Неожиданный символ {value!r} на строке {line_num}')
            
            # Добавляем токен в результат
            tokens.append({
                'type': kind,
                'value': value,
                'line': line_num,
                'column': column
            })
        
        return tokens


class LexerAnalyzer:
    """
    Анализатор для удобной работы с лексером.
    
    Предоставляет методы для анализа кода, вывода результатов
    и сбора статистики.
    """
    
    def __init__(self):
        """Инициализация анализатора."""
        self.lexer = PseudocodeLexer()
        self.tokens = []
    
    def analyze(self, code: str) -> List[Dict[str, Any]]:
        """
        Анализирует код и возвращает список токенов.
        
        Args:
            code (str): Исходный код для анализа
            
        Returns:
            List[Dict[str, Any]]: Список токенов с дополнительной информацией
        """
        self.tokens = []
        raw_tokens = self.lexer.tokenize(code)
        
        # Преобразуем токены в удобный формат
        for token in raw_tokens:
            token_info = {
                'text': str(token['value']),
                'type': token['type'],
                'line': token['line'],
                'column': token['column']
            }
            self.tokens.append(token_info)
        
        return self.tokens
    
    def analyze_file(self, file_path: str) -> List[Dict[str, Any]]:
        """
        Анализирует код из файла.
        
        Args:
            file_path (str): Путь к файлу с кодом
            
        Returns:
            List[Dict[str, Any]]: Список токенов
        """
        try:
            with open(file_path, 'r', encoding='utf-8') as f:
                code = f.read()
            return self.analyze(code)
        except FileNotFoundError:
            print(f"❌ Файл не найден: {file_path}")
            return []
        except Exception as e:
            print(f"❌ Ошибка чтения файла {file_path}: {e}")
            return []
    
    def print_tokens(self, title: str = "ЛЕКСИЧЕСКИЙ АНАЛИЗ"):
        """
        Выводит красиво отформатированную таблицу с токенами.
        
        Args:
            title (str): Заголовок для вывода
        """
        if not self.tokens:
            print("📭 Нет токенов для отображения")
            return
            
        print("=" * 80)
        print(title)
        print("=" * 80)
        print(f"{'ТОКЕН':<20} {'ТИП':<15} {'СТРОКА':<8} {'ПОЗИЦИЯ':<8}")
        print("-" * 80)
        
        for token in self.tokens:
            print(f"{token['text']:<20} {token['type']:<15} {token['line']:<8} {token['column']:<8}")
        
        print("-" * 80)
        print(f"📊 Всего токенов: {len(self.tokens)}")
    
    def get_statistics(self) -> Dict[str, int]:
        """
        Возвращает статистику по типам токенов.
        
        Returns:
            Dict[str, int]: Словарь {тип_токена: количество}
        """
        token_types = {}
        for token in self.tokens:
            token_type = token['type']
            token_types[token_type] = token_types.get(token_type, 0) + 1
        return token_types
    
    def print_statistics(self):
        """Выводит статистику по типам токенов."""
        stats = self.get_statistics()
        
        if not stats:
            print("📭 Нет данных для статистики")
            return
            
        print("\n📈 СТАТИСТИКА ПО ТИПАМ ТОКЕНОВ:")
        print("-" * 40)
        for token_type, count in sorted(stats.items()):
            print(f"  {token_type}: {count}")
        
        # Общая статистика
        total_tokens = len(self.tokens)
        unique_types = len(stats)
        most_common = max(stats, key=stats.get) if stats else "N/A"
        
        print(f"\n📊 ОБЩАЯ СТАТИСТИКА:")
        print(f"  Всего токенов: {total_tokens}")
        print(f"  Уникальных типов: {unique_types}")
        print(f"  Самый частый тип: {most_common} ({stats.get(most_common, 0)} раз)")


def demonstrate_capabilities():
    """
    Демонстрирует основные возможности лексического анализатора.
    """
    print("🎯 ДЕМОНСТРАЦИЯ ВОЗМОЖНОСТЕЙ ЛЕКСЕРА")
    print("=" * 60)
    
    test_cases = [
        ("Присваивание", "x = 42;"),
        ("Арифметика", "result = (a + b) * c / 2;"),
        ("Остаток от деления", "remainder = a % b;"),
        ("Сравнения", "if (x >= 10 && y != 5) { z = 1; }"),
        ("Цикл for", "for i in range(1, 10) { print(i); }"),
        ("Цикл while", "while (x > 0 || y < 100) { x = x - 1; }"),
        ("Строки", 'print("Hello, World!");'),
        ("Комментарии", "# Это комментарий\nx = 10; # И это тоже"),
    ]
    
    analyzer = LexerAnalyzer()
    
    for name, code in test_cases:
        print(f"\n🔹 {name}:")
        print(f"   Код: {code}")
        tokens = analyzer.analyze(code)
        token_descriptions = [f"{t['type']}({t['text']})" for t in tokens]
        print(f"   Токены: {token_descriptions}")
        print(f"   Всего токенов: {len(tokens)}")


def analyze_example_code():
    """
    Анализирует пример комплексного кода на псевдокоде.
    """
    print("\n" + "=" * 60)
    print("📊 АНАЛИЗ ПРИМЕРА КОМПЛЕКСНОГО КОДА")
    print("=" * 60)
    
    example_code = """# Простая программа на псевдокоде
# Вычисление суммы четных чисел

sum = 0;
n = 10;

for i in range(1, n + 1) {
    if (i % 2 == 0) {
        sum = sum + i;
        print("Added: " + i);
    }
}

print("Total sum: " + sum);

# Проверка результата
if (sum > 20) {
    print("Large sum!");
} else {
    print("Small sum.");
}
"""
    
    analyzer = LexerAnalyzer()
    tokens = analyzer.analyze(example_code)
    analyzer.print_tokens("АНАЛИЗ ПРИМЕРНОЙ ПРОГРАММЫ")
    analyzer.print_statistics()


def main():
    """
    Основная функция - демонстрация работы лексического анализатора.
    """
    print("🎯 ЛЕКСИЧЕСКИЙ АНАЛИЗАТОР ПСЕВДОКОДА - ЛР1")
    print("=" * 60)
    
    # Демонстрация возможностей
    demonstrate_capabilities()
    
    # Анализ комплексного примера
    analyze_example_code()
    
    print("\n" + "=" * 60)
    print("✅ ЛЕКСИЧЕСКИЙ АНАЛИЗАТОР РАБОТАЕТ КОРРЕКТНО!")
    print("=" * 60)


if __name__ == '__main__':
    try:
        main()
    except Exception as e:
        print(f"\n💥 КРИТИЧЕСКАЯ ОШИБКА: {e}")
        import traceback
        traceback.print_exc()
