## 🚀 Быстрый старт

### Установка и настройка
```bash
# Клонирование репозитория (если необходимо)
git clone <repository-url>
cd 009

# Создание виртуального окружения
python3 -m venv .venv

# Активация виртуального окружения
source .venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt
```
### Запуск проекта
```bash
# Проверка структуры проекта
python check_structure.py

# Запуск тестов
python tests/run_tests.py

# Демонстрация работы лексера
python src/lexer.py

# Генерируем парсер из грамматики (если используем ANTLR)
antlr4 -Dlanguage=Python3 src/grammars/Pseudocode.g4

# Запускаем тесты синтаксического анализатора
python tests/run_syntax_tests.py

# Демонстрируем работу анализатора
python src/analyzer.py
```
### 📁 Структура проекта
```text
009/
├── docs/                    # Документация
│   └── LAB1_REPORT.md      # Отчет по лабораторной работе
├── src/                    # Исходный код
│   ├── grammars/          # Грамматики ANTLR
│   │   └── Pseudocode.g4  # Основная грамматика
│   └── lexer.py           # Основной лексический анализатор
├── tests/                  # Тесты
│   ├── test_cases/        # Тестовые примеры
│   │   ├── basic.pseudo   # Базовые конструкции
│   │   ├── arithmetic.pseudo # Арифметические операции
│   │   └── loops.pseudo   # Циклы и условия
│   └── run_tests.py       # Тестовый раннер
├── examples/              # Примеры программ
│   ├── factorial.pseudo   # Вычисление факториала
│   └── max_finder.pseudo  # Поиск максимального числа
├── .gitignore            # Игнорируемые файлы
├── requirements.txt      # Зависимости Python
├── README.md            # Этот файл
└── check_structure.py   # Проверка структуры проекта
```
### 🎯 Возможности лексического анализатора
Поддерживаемые конструкции

1. Ключевые слова
	+ Управление потоком: if, else, while, for
	+ Функции: print, range
	+ Операторы: in
5. Операторы
	+ Арифметические: +, -, *, /, % (остаток от деления)
	+ Сравнения: ==, !=, <, >, <=, >=
	+ Логические: && (И), || (ИЛИ), ! (НЕ)
	+ Присваивание: =
6. Литералы и идентификаторы
	+ Числа: 42, 100, 0
	+ Строки: "Hello, World!", "текст"
	+ Идентификаторы: variable_name, result2, tempValue
10. Структуры управления
```pseudo
# Условные операторы
if (x > 5) {
    print("x is greater than 5");
} else {
    print("x is 5 or less");
}

# Циклы
for i in range(1, 10) {
    print("Iteration: " + i);
}

while (counter > 0) {
    counter = counter - 1;
}
```

5. Комментарии
```pseudo
# Это однострочный комментарий
x = 10; # Комментарий после кода
```
### 📊 Пример использования
#### Программный интерфейс
```python
from src.lexer import PseudocodeLexer, LexerAnalyzer

# Создание анализатора
analyzer = LexerAnalyzer()

# Анализ кода из строки
code = """
x = 10;
if (x > 5) {
    print("x is large");
}
"""
tokens = analyzer.analyze(code)

# Анализ кода из файла
tokens = analyzer.analyze_file("examples/factorial.pseudo")

# Вывод результатов
analyzer.print_tokens()
analyzer.print_statistics()
```
#### Пример вывода
```text
============================================================
ЛЕКСИЧЕСКИЙ АНАЛИЗ
============================================================
ТОКЕН               ТИП            СТРОКА   ПОЗИЦИЯ
------------------------------------------------------------
x                   ID             2        1
=                   ASSIGN         2        3
10                  NUMBER         2        5
;                   SEMI           2        7
if                  IF             3        1
(                   LPAREN         3        4
x                   ID             3        5
>                   GT             3        7
5                   NUMBER         3        9
...
------------------------------------------------------------
Всего токенов: 25

📈 СТАТИСТИКА ПО ТИПАМ ТОКЕНОВ:
  ID: 5
  ASSIGN: 3
  NUMBER: 4
  IF: 1
  ...
🧪 Тестирование
Запуск всех тестов
bash
python tests/run_tests.py
Тестовое покрытие
✅ Базовые конструкции (присваивание, арифметика)

✅ Операторы (сравнения, логические, арифметические)

✅ Ключевые слова (все зарезервированные слова)

✅ Файловый ввод/вывод (анализ из файлов)

✅ Обработка ошибок (неизвестные символы)

✅ Комментарии и пробелы (корректное игнорирование)

Результаты тестирования
🎯 ОБЩИЙ РЕЗУЛЬТАТ: 25/25 (100.0%)
🎉 ВСЕ ТЕСТЫ ПРОЙДЕНЫ УСПЕШНО!
```
### 🔧 Разработка
#### Добавление нового токена
+ Добавьте определение в ``TOKEN_SPECIFICATION`` в ``src/lexer.py``
+ Обновите грамматику в ``src/grammars/Pseudocode.g4``
+ Добавьте тесты в ``tests/run_tests.py``
+ Запустите тесты для проверки

#### Пример добавления оператора
```python
# В TOKEN_SPECIFICATION
('POWER', r'\*\*'),  # Возведение в степень

# В соответствующем месте (после MUL, перед DIV)
```
### 📋 Требования
#### Системные требования
  + Python: 3.8+
  + Память: 128 MB RAM
  + Дисковое пространство: 10 MB

#### Зависимости
  + ``antlr4-python3-runtime: >=4.13.2``

### 🐛 Отладка и решение проблем
#### Частые проблемы
>Команда «python» не найдена
>>Решение: Используйте python3 или установите python-is-python3

>Ошибки импорта модулей
>>Решение: Убедитесь, что вы в виртуальном окружении и зависимости установлены

>Неожиданные символы
>>Решение: Проверьте, что все операторы добавлены в лексер

#### Логирование и отладка
```python
# Включение подробного вывода
analyzer.print_tokens("Детальный анализ")
```

